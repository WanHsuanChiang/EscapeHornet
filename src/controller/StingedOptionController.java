package controller;

import javafx.event.EventHandler;
import javafx.scene.Node;
import javafx.scene.input.InputEvent;

public class StingedOptionController implements EventHandler<InputEvent>{

	@Override
	public void handle(InputEvent event) {
		Node option = (Node) event.getSource();
		
	}

}
